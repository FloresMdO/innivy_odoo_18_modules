<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Action Window-->
    <record id="hive_config_action" model="ir.actions.act_window">
        <field name="name">Hive Configuration</field>
        <field name="res_model">hive.config</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Tree View-->
    <record id="view_hive_config_tree" model="ir.ui.view">
        <field name="name">hive.config.list</field>
        <field name="model">hive.config</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="is_hive_enabled" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_hive_config_form" model="ir.ui.view">
        <field name="name">hive.config.form</field>
        <field name="model">hive.config</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="My Hive Connector"/>
                        </h1>
                    </div>
                    <notebook>
                        <page name="Configuration" string="Configuration">
                            <group>
                                <group>
                                    <field name="hive_url"/>
                                    <field name="hive_token"/>
                                </group>
                                <group>
                                    <field name="is_hive_enabled" widget="boolean_toggle"/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Menu Item -->
    <menuitem id="hive_config_menu_root" name="HIVE" web_icon="innivy_hive_connector,static/description/icon.png">
        <menuitem id="hive_config_menu" action="hive_config_action"/>
    </menuitem>
</odoo>